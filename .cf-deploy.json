{
  "packageManager": "npm",
  "targets": [
    { "dir": "site", "build": "SITE_URL=https://commentnavigation.plugins.topxup.com npm run build", "deploy": "npx wrangler deploy", "type": "workers", "name": "commentnavigation-site", "url": "https://commentnavigation.plugins.topxup.com" }
  ],
  "notes": ["Static Astro site on Workers Static Assets; the passthrough worker in site/worker/index.ts serves ./dist.", "SITE_URL decides canonical/hreflang/sitemap — changing the domain means rebuilding."]
}
