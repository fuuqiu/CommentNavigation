-- # 角色与权限核对
-- 普通说明不进入标题大纲；这里使用常量演示，不访问业务数据。

-- ## 角色本体
-- ### 系统模板
SELECT 'system role' AS example;

-- ### 商户角色
SELECT 'merchant role' AS example;

-- ## 授权明细
/*
 * ### 页面权限
 * #### PRO 页面
 */
SELECT 'page permission' AS example;

-- ### 模块关联
SELECT 'module relation' AS example;

-- # 结果对照
-- ## 变更前后
SELECT 'comparison' AS example;
